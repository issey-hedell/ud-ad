# secrets-candidates

このフォルダには `scripts/discover-secrets.sh --copy` によって作成された **サニタイズ済み** の候補ファイルが格納されます。

注意:
- 値は `<REDACTED>` でマスクされています。実際の値はこのリポジトリに入れないでください。
- 管理者が `scripts/bootstrap-secrets.sh` を使い、ローカルの `.env.secrets` をもとに org-level secret として登録してください。
